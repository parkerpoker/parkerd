{
  "name": "@parker/daemon-runtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "test": "vitest run",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  },
  "dependencies": {
    "@parker/game-engine": "file:../game-engine",
    "@parker/protocol": "file:../protocol",
    "@parker/settlement": "file:../settlement",
    "ws": "^8.18.3"
  }
}
